# financial_cnn_forecast
This project implements financial time series forecasting using Short-Time Fourier Transform (STFT) and Convolutional Neural Networks (CNN). The model converts stock price data into spectrogram representations and performs both one-step and multi-step future predictions using recursive forecasting techniques.
